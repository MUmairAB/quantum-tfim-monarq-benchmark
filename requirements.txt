# Top-level dependencies; pip resolves the rest. Tested on macOS/CPU.
# numpy pinned to match Compute Canada's wheelhouse.
numpy==2.4.2
matplotlib==3.11.1
PyYAML==6.0.3

# MonarQ connectivity (Qiskit path)
# qiskit 2.5.0 — newest version with a Compute Canada wheel.
qiskit==2.5.0
qiskit-calculquebec @ git+https://github.com/calculquebec/qiskit-calculquebec.git@d21bbb75997b70f9ba269b05f4707aa7173d07a5
python-dotenv==1.2.3

# MonarQ connectivity (PennyLane path) + VQE
pennylane==0.45.1
pennylane-calculquebec==0.8.2
# VQE (src/vqe.py) runs on lightning.qubit with adjoint differentiation.
optax==0.2.8
pennylane-lightning==0.45.0

# NNQS
netket==3.22.3
# jax 0.10.2 — 0.11.0 needs Python >=3.12, Compute Canada runs 3.11.
jax==0.10.2
# On a GPU machine, install jax[cuda12] instead for GPU-accelerated NNQS training.

# Exact diagonalization (needs OpenMP at the system level — on macOS: brew install libomp)
quspin==1.0.1

# Notebooks
jupyter==1.1.1
ipykernel==7.3.0
